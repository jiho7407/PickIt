# Figma Batch1 Design System References

These PNGs are saved visual references from the Figma component library nodes the user provided on 2026-05-07.

Use them as implementation references when building future UI. Prefer existing code-native components for simple UI, but use these captures to preserve exact layout, spacing, colors, and grouped artwork. Do not recreate bitmap-like Figma artwork by hand when an export should be used.

See `manifest.json` for node IDs, source grouping, included components, and repeated design tokens.
